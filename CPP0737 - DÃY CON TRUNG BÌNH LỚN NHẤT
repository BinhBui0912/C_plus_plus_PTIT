#include<bits/stdc++.h>

using namespace std;

int main(){
	int t;
	cin >> t;
	while(t--){
		int n, k;
		cin >> n >> k;
		int a[n];
		for(int i = 0; i < n; i++) cin >> a[i];
		long long sum = 0;
		for(int i = 0; i < k; i++){
			sum += a[i];
		}
		long long res = sum, tmp = 0;
		for(int i = k; i < n; i++){
			sum = sum - a[i-k] + a[i];
			if(sum > res){
				res = sum;
				tmp = i-k+1;
			}
		}
		for(int j = tmp; j < tmp+k; j++){
			cout << a[j] << " ";
		}
		cout << endl;
}
}

